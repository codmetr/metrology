<UserControl x:Class="KipTM.Checks.View.PressureSensorReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewer="clr-namespace:SAPBusinessObjects.WPF.Viewer;assembly=SAPBusinessObjects.WPF.Viewer"
             xmlns:local="clr-namespace:KipTM.Checks.View"
             xmlns:attachedCommandBehavior="clr-namespace:AttachedCommandBehavior;assembly=Tools.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Skins/FluentSkin.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="20">
        <TabControl TabStripPlacement="Left">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate DataType="TabItem">
                                <Border Height="50" Width="160">
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Style="{StaticResource TblHeaderStyle}" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Header="Свидетельство о поверке">
                <viewer:CrystalReportsViewer />
            </TabItem>
            <TabItem>
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <Border Height="50" Width="160">
                            <TextBlock Text="Протокол поверки" TextWrapping="Wrap"
                                       Style="{StaticResource TblHeaderStyle}" VerticalAlignment="Center">
                                <attachedCommandBehavior:CommandBehaviorCollection.Behaviors>
                                    <attachedCommandBehavior:BehaviorBinding Event="MouseLeftButtonDown" Command="{Binding LoadedCommand}"/>
                                </attachedCommandBehavior:CommandBehaviorCollection.Behaviors>
                            </TextBlock>
                        </Border>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <viewer:CrystalReportsViewer/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
