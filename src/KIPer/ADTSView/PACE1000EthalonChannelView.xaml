<UserControl x:Class="KipTM.View.Checks.PACE1000EthalonChannelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:channels="clr-namespace:KipTM.ViewModel.Channels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance channels:PACEEchalonChannelViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="1" Grid.Row="1" Text="Текущее давление: "/>
        <TextBlock Grid.Column="2" Grid.Row="1" Text="{Binding Pressure, UpdateSourceTrigger=PropertyChanged}" MinWidth="100"/>
        <TextBlock Grid.Column="3" Grid.Row="1" Text="{Binding PressureUnit, UpdateSourceTrigger=PropertyChanged}" MinWidth="50"/>
    </Grid>
</UserControl>
